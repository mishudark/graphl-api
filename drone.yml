kind: pipeline
name: bazel

steps:
- name: test
  image: mishudark/bazel:1.1
  depends_on: [ clone ]
  volumes:
  - name: cache
    path: /root/bazel
  commands:
  - make bazel-test
  - make bazel-lint
  - make bazel-build

volumes:
- name: cache
  host:
    path: /mnt/stateful_partition/tmp/
